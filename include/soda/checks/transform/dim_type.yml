checks for dim_type:
  - schema:
      fail:
        when required column missing: 
          [dim_type_key, pokemon_id]
        when wrong column type:
          dim_type_key: string
          pokemon_id: int
          primary_type: string
          secondary_type: string