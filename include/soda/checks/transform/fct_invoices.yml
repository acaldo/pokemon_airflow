checks for fct_pokemon:
  - schema:
      fail:
        when required column missing: 
          [id,name,weight,height,dim_type_key,dim_move_key]
        when wrong column type:
          dim_move_type: string
          id: int
          name: string
          weight: int
          height: int
          dim_type_key: string
          dim_move_key: string
